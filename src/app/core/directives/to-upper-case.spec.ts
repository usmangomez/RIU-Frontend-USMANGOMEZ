import { ToUpperCase } from './to-upper-case';

describe('ToUpperCase', () => {
  it('should create an instance', () => {
    const directive = new ToUpperCase();
    expect(directive).toBeTruthy();
  });
});
